# QuantumCipherSim - Python Dependencies
# Install: pip install -r requirements.txt
#
# IMPORTANT: scikit-learn is pinned to an EXACT version because the trained
# .joblib model files are pickle-format and contain references to internal
# scikit-learn Cython modules (e.g. _loss). Upgrading scikit-learn across
# major versions can rename/remove these modules, breaking model loading
# at startup. Only change this pin after retraining and re-committing all
# model files in ml/*.joblib.

# ── Quantum Simulation ────────────────────────────────────────────────────
qiskit>=1.0.0
qiskit-aer>=0.14.0

# ── ML Pipeline ───────────────────────────────────────────────────────────
scikit-learn==1.4.2          # PINNED — must match version used for training
numpy>=1.26.0,<2.0.0         # numpy 2.x has breaking changes for some models
pandas>=2.0.0
joblib>=1.3.0,<1.5.0

# ── Backend (FastAPI) ─────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# ── Database ──────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9        # PostgreSQL driver

# ── Utilities ─────────────────────────────────────────────────────────────
python-multipart>=0.0.9       # FastAPI file upload support (if needed later)
