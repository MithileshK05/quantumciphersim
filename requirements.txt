# QuantumCipherSim - Python Dependencies
# Install: pip install -r requirements.txt
#
# Python version: 3.11.9 (pinned via .python-version and runtime.txt)
#
# IMPORTANT: scikit-learn is bounded to <1.5 because the trained .joblib
# model files contain pickle references to internal scikit-learn Cython
# modules. Upgrading across minor versions can rename these modules and
# break model loading at startup. Only change this bound after retraining
# and re-committing all ml/*.joblib files.

# ── Quantum Simulation ────────────────────────────────────────────────────
qiskit>=1.0.0
qiskit-aer>=0.14.0

# ── ML Pipeline ───────────────────────────────────────────────────────────
scikit-learn>=1.8.0          # bounded — must match version 1.8.0 used for training
numpy>=1.26.0,<2.0.0          # numpy 2.x has breaking API changes
pandas>=2.0.0
joblib>=1.4.0

# ── Backend (FastAPI) ─────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# ── Database ──────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9        # PostgreSQL driver

# ── Utilities ─────────────────────────────────────────────────────────────
python-multipart>=0.0.9       # FastAPI file upload support
